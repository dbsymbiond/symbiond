# Node.js common (apply to all Node.js projects/services)
node_modules/
npm-debug.log*
yarn-debug.log*
.npm/

# Build artifacts (for both frontend and backend)
dist/
build/
web-build/ # If used by any project (e.g., frontend)

# Docker specific (crucial for local development, not committed)
.dockerignore
**/*.flag
pgdata/

# Environment variables (sensitive info, never commit)
.env
env

# Logs and temporary files
*.log
tmp/
logs/

# Operating System and IDE files (general to any project)
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp # Vim swap files
*.bak # Backup files

# Test artifacts
coverage/
__snapshots__/

# Certificates (if used for local HTTPS development)
*.pem
*.key
*.crt

# Executables or compiled binaries (if any services compile to native)
*.exe
*.dll
*.so
*.o

# Expo (React Native)
.expo/